---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: secretusages.usage.secretusage.io
spec:
  group: usage.secretusage.io
  names:
    kind: SecretUsage
    listKind: SecretUsageList
    plural: secretusages
    shortNames:
    - su
    singular: secretusage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.secretName
      name: Secret
      type: string
    - jsonPath: .status.exists
      name: Exists
      type: boolean
    - jsonPath: .status.usageCount
      name: Usages
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SecretUsage is a namespaced inventory object for one Secret's
          references.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecretUsageSpec is intentionally empty. The controller owns
              SecretUsage status.
            type: object
          status:
            description: SecretUsageStatus describes where a namespaced Secret is
              referenced.
            properties:
              exists:
                description: Exists indicates whether the Secret currently exists
                  in the namespace.
                type: boolean
              secretName:
                description: SecretName is the Secret represented by this SecretUsage
                  object.
                type: string
              usageCount:
                description: UsageCount is the number of entries in Usages.
                format: int32
                type: integer
              usages:
                description: Usages lists Kubernetes objects and fields that reference
                  the Secret.
                items:
                  description: SecretUsageReference identifies a single reference
                    from an object to a Secret.
                  properties:
                    apiVersion:
                      type: string
                    container:
                      type: string
                    fieldPath:
                      type: string
                    key:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    optional:
                      type: boolean
                    uid:
                      type: string
                  required:
                  - apiVersion
                  - fieldPath
                  - kind
                  - name
                  - namespace
                  type: object
                type: array
            required:
            - exists
            - usageCount
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
