apiVersion: v2
name: secretusage-controller
description: A Kubernetes controller that builds a live reverse index of Secret references across workloads, ServiceAccounts and Ingresses.
type: application
version: 0.3.1
appVersion: "0.3.0"
